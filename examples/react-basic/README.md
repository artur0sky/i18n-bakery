# Tasting Sample (React Basic) 🍰

A simple slice of cake demonstrating how to use `i18n-bakery` in a React application with Vite.

## 📋 Recipe

- **Vite**: For fast baking.
- **React**: For the structure.
- **i18n-bakery**: For the flavor.

## 👩‍🍳 Instructions

1. **Install dependencies:**
   ```bash
   pnpm install
   ```

2. **Start the oven (dev server):**
   ```bash
   pnpm dev
   ```

3. **Taste it:**
   Open `http://localhost:5173` and try switching languages.

## 📝 Key Ingredients

Check out `src/main.tsx` to see how the `I18nProvider` is configured with a custom loader.
